[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
[project]
name = "home-ray"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []
[project.optional-dependencies]
node = ["ray[default]==2.58.0"]
storage = ["boto3>=1.35,<2"]
[tool.setuptools]
packages = ["home_ray"]
